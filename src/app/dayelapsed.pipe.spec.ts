import { DayelapsedPipe } from './dayelapsed.pipe';

describe('DayelapsedPipe', () => {
  it('create an instance', () => {
    const pipe = new DayelapsedPipe();
    expect(pipe).toBeTruthy();
  });
});
